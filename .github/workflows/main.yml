name: "Gachi_bot update"
on:
  push:
    branches: ["async"]
  pull_request:
    branches: ["async"]
    
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: pull_updates 🛎
        uses: ./
        with:
          command: git pull
          working-directory: gachi_bot/gachi_bot
      - name: run
        uses: ./
        with:
          command: run.sh
          working-directory: gachi_bot/gachi_bot
        
      
