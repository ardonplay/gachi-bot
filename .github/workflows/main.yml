name: Run script on commit

on:
  push:
    branches:
      - async

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run script
        run: ./run.sh
